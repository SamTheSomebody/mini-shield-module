/*&uart0 {
    pinctrl-0 = <&uart0_default>;
    pinctrl-names = "default";
};

&pinctrl {
    //configuration for uart0 device, default state
    uart0_default_left: uart0_default {
        group1 {
            psels = <NRF_PSEL(UART_RX, 0, 21)>;
        };
    uart0_default_right: uart0_default {
        group1 {
            psels = <NRF_PSEL(UART_TX, 0, 21)>, <NRF_PSEL(UART_CTS, 0, 4)>;
        };
    };
};*/
